//先假设字符串均由大写字母A 〜 Z 构成。在这个基础上，不妨把A 〜Z 视为0 〜 2 5 , 这样就把2 6个大写字母对应到了二十六进制中。
// //int hashFunc (char S[], int len) { //hash 函数，将字符串 S 转换为整数
// int id = 0;
// for (int i = 0; i < len; i++} {
// i d = i d * 2 6 + (S[i] - fA*) ； / /将二十六进制转换为十进制
// r e t u r n id;
// }



// //如果字符串S 的长度比较长，那么转换成的整数也会很大，因此需要注意使用时
// len不能太长。如果字符串中出现了小写字母，那么可以把A 〜 Z 作为0 〜 25,而把a 〜 z 作为
// 2 6 〜 51，这样就变成了五十二进制转换为十进制的问题



#in